260131_lz对比实验/
├── checkpoints/          # 存放训练好的模型权重文件 (.pth, .ckpt)
├── data/                 # 存放原始数据文件 (如 CSV, Excel, NPZ 等)
├── datasets/             # PyTorch Dataset 和 DataLoader 的实现逻辑
├── docs/                 # 项目文档、参考文献、需求说明
├── experiments/          # 实验日志、TensorBoard 记录、不同配置的实验结果
├── inference/            # 推理脚本，用于对新数据进行填充预测
├── losses/               # 自定义损失函数 (如 Masked MSE, Adversarial Loss 等)
├── models/               # 深度学习模型架构定义 (如 GAIN, Transformer, Unet 等)
├── output/               # 存放填充完成后的结果数据
├── postprocessing/       # 后处理脚本 (如数据反归一化、评估指标计算)
├── preprocessing/        # 预处理脚本 (如缺失值构造、归一化、特征工程)
├── scripts/              # 辅助自动化脚本 (如一键运行脚本、数据下载脚本)
├── training/             # 训练逻辑实现 (训练循环、优化器设置、验证逻辑)
└── visualization/        # 绘图脚本 (填充效果对比图、热力图、收敛曲线等)